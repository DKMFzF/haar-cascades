version: '3'

tasks:
  run:
    dir: '{{.USER_WORKING_DIR}}'
    cmds:
      - ./venv/bin/python3 src/main.py

  install:
    dir: '{{.USER_WORKING_DIR}}'
    cmds:
      - rm -rf venv
      - python3 -m venv venv
      - ./venv/bin/python3 -m pip install -r requirements.txt