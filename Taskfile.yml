version: '3'

tasks:
  run:
    dir: '{{.USER_WORKING_DIR}}'
    cmds:
      - python3 src/main.py

  install:
    dir: '{{.USER_WORKING_DIR}}'
    cmds:
      - python3 -m venv venv
      - venv/bin/pip install -r requirements.txt