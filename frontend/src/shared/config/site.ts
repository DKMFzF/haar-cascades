export const siteConfig = {
  title: "CV Face Detection",
  description: "Realtime face detection demo",
};

export const titleSiteTemplate = (title: string): string => {
  return `%s — ${title}`;
};
