import type { Metadata } from "next";

export { HomePage } from "./ui/home-page";

export const metadata: Metadata = {
  title: "Face Detector",
  description: "Realtime face detection via WebSocket",
};
